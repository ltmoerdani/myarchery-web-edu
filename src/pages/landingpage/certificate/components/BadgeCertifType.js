import styled from "styled-components";

const BadgeCertifType = styled.div`
  padding: 0.47rem 1rem;
  border-radius: 1rem;

  background-color: var(--bs-info);
  color: #ffffff;

  text-transform: capitalize;
  font-weight: 400;
  line-height: 1.5;
  text-align: center;
  vertical-align: middle;
`;

export default BadgeCertifType;
