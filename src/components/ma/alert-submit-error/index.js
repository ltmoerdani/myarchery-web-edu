import { AlertServerError as AlertSubmitError } from "components/ma";

/**
 * Implementasinya dipindah ke komponen `AlertServerError`.
 * Di sini cuma diekspor ulang pakai nama yang sebelumnya
 * supaya yang masih panggil komponen sebelumnya tetap kompatibel.
 */

// TODO: hapus file ini

export { AlertSubmitError };
